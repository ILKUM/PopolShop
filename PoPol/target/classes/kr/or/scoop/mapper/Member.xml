<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.scoop.dao.MemberDao">
	<!-- 일반회원가입 -->
    <insert id="insertMember" parameterType="kr.or.scoop.dto.Member">
       insert into member(email, pwd, name,iddate,loginnum)
       values (#{email},#{pwd},#{name},SYSDATE,1)
    </insert>
	<!-- 회원가입 중복체크 -->
	<select id="loginCheck" resultType="int">
		select count(*)
		from member 
		where email = #{param1} and pwd = #{param2}
	</select>
	    <!-- 회원가입 아이디 중복체크 -->
    <select id="idCheck" resultType="int">
		select count(*)
		from member 
		where email = #{param1}
	</select>
</mapper>